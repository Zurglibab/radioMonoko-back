export interface WebRadio {
    id: string;
    title: string;
    description?: string;
    liveStream?: string;
    playerUrl?: string;
}

export interface LocalRadio {
    id: string;
    title: string;
    description?: string;
    liveStream?: string;
    playerUrl?: string;
}

